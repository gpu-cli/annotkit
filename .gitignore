.build/
.swiftpm/
.claude/worktrees/
*.xcodeproj
*.xcworkspace
DerivedData/
.DS_Store
Package.resolved

# Demo/practice artifacts: AnnotKitDemo writes notes into the working directory
ANNOTKIT_NOTES.md
ANNOTKIT_NOTES.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Local secrets. NEVER commit these — .env carries a live Resend key.
.env
.env.*
!.env.example
